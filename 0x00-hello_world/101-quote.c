#include<stdio.h>

/**
* main -Entry point
* display a message
* Return: Always 0(Success)
*/
int main(void)
{
	fprintf(stderr, "with proper grammar, but the outcome is a piece of art,\n");
	return (1);
}
